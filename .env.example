# VITE_API_URL: The URL of the backend server.
# If deploying frontend to Netlify, set this to your backend URL (e.g., on Render/Heroku).
# If running locally or in a full-stack container, leave empty to default to window.location.origin.
VITE_API_URL=""
